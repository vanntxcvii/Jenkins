@Library('my-shared-library') _

pipeline {
    agent any
    
    stages {
        stage('Print Greeting') {
            steps {
                printGreeting('World')
            }
        }
    }
}